# cognito-login

```
export REGION=xxx
export USER_POOL_ID=xxx
export CLIENT_ID=xxx
node index.js -u username -p password
```

```
export REGION=xxx
export USER_POOL_ID=xxx
export CLIENT_ID=xxx
node index.js -u username -p password --new-password newpassword
```

